# Django Configuration
SECRET_KEY=django-insecure-change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration (PostgreSQL)
# If PostgreSQL variables are not set or unavailable, SQLite is used as local fallback.
DB_ENGINE=django.db.backends.postgresql
DB_NAME=ai_recruiter_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# LLM API Configuration (Used exclusively server-side; NEVER exposed to frontend JavaScript)
LLM_API_KEY=your_llm_api_key_here
LLM_MODEL=gemini-1.5-flash

# Session Security (Timeout in seconds, default: 3600 = 1 hour)
SESSION_COOKIE_AGE=3600
SESSION_EXPIRE_AT_BROWSER_CLOSE=True
